# Task 1 - Rule-Based Chatbot

A simple chatbot built in Python using if-else statements and basic
pattern matching to respond to user inputs.

## How to run
1. Make sure Python is installed
2. Run: python chatbot.py
3. Type messages to chat with the bot, type 'bye' to exit